diff --git a/configs/example/arm/arm_multicore_d9200.py b/configs/example/arm/arm_multicore_d9200.py
index 18ae984fdb..2e0f95c0b9 100644
--- a/configs/example/arm/arm_multicore_d9200.py
+++ b/configs/example/arm/arm_multicore_d9200.py
@@ -70,6 +70,16 @@ from common.cores.arm import (
 )
 
 default_disk = "ubuntu-18.04-arm64-docker.img"
+default_coral_repo = os.path.abspath(
+    os.path.join(
+        os.path.dirname(__file__),
+        "..",
+        "..",
+        "..",
+        "third_party",
+        "coralnpu",
+    )
+)
 
 default_mem_size = "4GiB"
 
@@ -230,6 +240,12 @@ def addOptions(parser):
         default=None,
         help="Restore from checkpoint",
     )
+    parser.add_argument(
+        "--exit-after-checkpoint",
+        action="store_true",
+        default=False,
+        help="Save one checkpoint and exit immediately after it is taken",
+    )
     parser.add_argument(
         "--dtb", type=str, default=None, help="DTB file to load"
     )
@@ -457,24 +473,54 @@ def addOptions(parser):
         help="add mem monitor to out port of slc"
     )
     
-    # NPU integration (stub model)
+    # Coral NPU stage-A integration
     parser.add_argument(
         "--enable-npu",
         action="store_true",
         default=False,
-        help="Instantiate a simple stub NPU device"
+        help="Instantiate the stage-A Coral NPU model"
     )
     parser.add_argument(
         "--npu-pio-addr",
         type=lambda v: int(v, 0),
         default=0x1D000000,
-        help="MMIO base address for the NPU device (default: 0x1D000000)"
+        help="Base address for the Coral NPU aperture (default: 0x1D000000)"
     )
     parser.add_argument(
         "--npu-pio-size",
         type=lambda v: int(v, 0),
-        default=0x1000,
-        help="MMIO size for the NPU device (default: 0x1000)"
+        default=0x31000,
+        help="Coral NPU aperture size (default: 0x31000)"
+    )
+    parser.add_argument(
+        "--npu-exec-latency",
+        default="1us",
+        help="Stage-A Coral NPU fixed run-to-halt latency (default: 1us)"
+    )
+    parser.add_argument(
+        "--npu-backend",
+        type=str,
+        choices=["stage-a", "verilated-coral"],
+        default="stage-a",
+        help="Coral backend implementation to use"
+    )
+    parser.add_argument(
+        "--npu-coral-repo",
+        type=str,
+        default=default_coral_repo,
+        help="Path to external google-coral/coralnpu checkout"
+    )
+    parser.add_argument(
+        "--npu-verilated-wrapper",
+        type=str,
+        default="",
+        help="Path to generated Verilated Coral wrapper artifacts"
+    )
+    parser.add_argument(
+        "--npu-rtl-tick-period",
+        type=str,
+        default="1ns",
+        help="Tick quantum for future Verilated Coral backend"
     )
 
     # Gemmini/NDP integration
@@ -659,9 +705,7 @@ def build(options):
             m5.options.outdir, "system.dtb"
         )
         system.generateDtb(system.workload.dtb_filename)
-    # Optionally instantiate a simple NPU stub device.
-    # The NPU is modeled as a generic DMA-capable MMIO device hanging off
-    # the IO bus. Its internal behavior is intentionally minimal.
+    # Optionally instantiate the stage-A Coral NPU model.
     if getattr(options, "enable_npu", False):
         # Import here to avoid hard dependency if the object is not built.
         from m5.objects import NPUDevice
@@ -669,6 +713,11 @@ def build(options):
         system.npu = NPUDevice(
             pioAddr=options.npu_pio_addr,
             pioSize=options.npu_pio_size,
+            executionLatency=options.npu_exec_latency,
+            backendType=options.npu_backend,
+            coralRepo=options.npu_coral_repo,
+            verilatedWrapper=options.npu_verilated_wrapper,
+            rtlTickPeriod=options.npu_rtl_tick_period,
         )
         # Attach MMIO port to the IO bus, similar to other devices.
         system.npu.pio = system.iobus.mem_side_ports
@@ -823,9 +872,15 @@ def run(options):
             exit_msg = event.getCause()
             if exit_msg == "checkpoint":
                 print("Dropping checkpoint at tick %d" % m5.curTick())
-                cpt_dir = os.path.join(options.ckpt_dir, "cpt.%d" % m5.curTick())
+                if options.exit_after_checkpoint:
+                    cpt_dir = os.path.join(options.ckpt_dir, "booted")
+                else:
+                    cpt_dir = os.path.join(options.ckpt_dir, "cpt.%d" % m5.curTick())
                 m5.checkpoint(cpt_dir)
                 print("Checkpoint done.")
+                if options.exit_after_checkpoint:
+                    print("Exiting after checkpoint as requested.")
+                    break
             elif "workbegin" in exit_msg:
                 print("m5 work begin dump and reset")
                 m5.stats.reset()
diff --git a/configs/example/arm/arm_multicore_d9300.py b/configs/example/arm/arm_multicore_d9300.py
index 355e9227ff..f369e031df 100644
--- a/configs/example/arm/arm_multicore_d9300.py
+++ b/configs/example/arm/arm_multicore_d9300.py
@@ -69,6 +69,16 @@ from common.cores.arm import (
 )
 
 default_disk = "ubuntu-18.04-arm64-docker.img"
+default_coral_repo = os.path.abspath(
+    os.path.join(
+        os.path.dirname(__file__),
+        "..",
+        "..",
+        "..",
+        "third_party",
+        "coralnpu",
+    )
+)
 
 default_mem_size = "4GiB"
 
@@ -227,6 +237,12 @@ def addOptions(parser):
         default=None,
         help="Restore from checkpoint",
     )
+    parser.add_argument(
+        "--exit-after-checkpoint",
+        action="store_true",
+        default=False,
+        help="Save one checkpoint and exit immediately after it is taken",
+    )
     parser.add_argument(
         "--dtb", type=str, default=None, help="DTB file to load"
     )
@@ -453,24 +469,54 @@ def addOptions(parser):
         default=False,
         help="add mem monitor to out port of slc"
     )
-    # NPU integration (stub model)
+    # Coral NPU stage-A integration
     parser.add_argument(
         "--enable-npu",
         action="store_true",
         default=False,
-        help="Instantiate a simple stub NPU device"
+        help="Instantiate the stage-A Coral NPU model"
     )
     parser.add_argument(
         "--npu-pio-addr",
         type=lambda v: int(v, 0),
         default=0x1D000000,
-        help="MMIO base address for the NPU device (default: 0x1D000000)"
+        help="Base address for the Coral NPU aperture (default: 0x1D000000)"
     )
     parser.add_argument(
         "--npu-pio-size",
         type=lambda v: int(v, 0),
-        default=0x1000,
-        help="MMIO size for the NPU device (default: 0x1000)"
+        default=0x31000,
+        help="Coral NPU aperture size (default: 0x31000)"
+    )
+    parser.add_argument(
+        "--npu-exec-latency",
+        default="1us",
+        help="Stage-A Coral NPU fixed run-to-halt latency (default: 1us)"
+    )
+    parser.add_argument(
+        "--npu-backend",
+        type=str,
+        choices=["stage-a", "verilated-coral"],
+        default="stage-a",
+        help="Coral backend implementation to use"
+    )
+    parser.add_argument(
+        "--npu-coral-repo",
+        type=str,
+        default=default_coral_repo,
+        help="Path to external google-coral/coralnpu checkout"
+    )
+    parser.add_argument(
+        "--npu-verilated-wrapper",
+        type=str,
+        default="",
+        help="Path to generated Verilated Coral wrapper artifacts"
+    )
+    parser.add_argument(
+        "--npu-rtl-tick-period",
+        type=str,
+        default="1ns",
+        help="Tick quantum for future Verilated Coral backend"
     )
 
     # Gemmini/NDP integration
@@ -657,9 +703,7 @@ def build(options):
         )
         system.generateDtb(system.workload.dtb_filename)
 
-    # Optionally instantiate a simple NPU stub device.
-    # The NPU is modeled as a generic DMA-capable MMIO device hanging off
-    # the IO bus. Its internal behavior is intentionally minimal.
+    # Optionally instantiate the stage-A Coral NPU model.
     if getattr(options, "enable_npu", False):
         # Import here to avoid hard dependency if the object is not built.
         from m5.objects import NPUDevice
@@ -667,6 +711,11 @@ def build(options):
         system.npu = NPUDevice(
             pioAddr=options.npu_pio_addr,
             pioSize=options.npu_pio_size,
+            executionLatency=options.npu_exec_latency,
+            backendType=options.npu_backend,
+            coralRepo=options.npu_coral_repo,
+            verilatedWrapper=options.npu_verilated_wrapper,
+            rtlTickPeriod=options.npu_rtl_tick_period,
         )
         # Attach MMIO port to the IO bus, similar to other devices.
         system.npu.pio = system.iobus.mem_side_ports
@@ -821,9 +870,15 @@ def run(options):
             exit_msg = event.getCause()
             if exit_msg == "checkpoint":
                 print("Dropping checkpoint at tick %d" % m5.curTick())
-                cpt_dir = os.path.join(options.ckpt_dir, "cpt.%d" % m5.curTick())
+                if options.exit_after_checkpoint:
+                    cpt_dir = os.path.join(options.ckpt_dir, "booted")
+                else:
+                    cpt_dir = os.path.join(options.ckpt_dir, "cpt.%d" % m5.curTick())
                 m5.checkpoint(cpt_dir)
                 print("Checkpoint done.")
+                if options.exit_after_checkpoint:
+                    print("Exiting after checkpoint as requested.")
+                    break
             elif "workbegin" in exit_msg:
                 print("m5 work begin dump and reset")
                 m5.stats.reset()
diff --git a/run_multicore.sh b/run_multicore.sh
index bd193da857..c36b3b2678 100755
--- a/run_multicore.sh
+++ b/run_multicore.sh
@@ -1,10 +1,124 @@
-export IMAGE_PATH=$(pwd)/image
-export M5_PATH="$(pwd)/image"
+#!/bin/sh
+
+set -e
+
+export IMAGE_PATH="$(pwd)/image"
+export M5_PATH="${IMAGE_PATH}"
+export CORAL_CKPT_BOOTSCRIPT="$(pwd)/configs/coralnpu/boot-to-checkpoint.rcS"
+export CORAL_RESUME_BOOTSCRIPT="$(pwd)/configs/coralnpu/fs-run.rcS"
+export CORAL_KERNEL_INIT="${CORAL_KERNEL_INIT:-/sbin/gem5-init.sh}"
+export BUILD_JOBS="$(getconf _NPROCESSORS_ONLN 2>/dev/null || sysctl -n hw.ncpu 2>/dev/null || echo 8)"
+export CORAL_DISK_IMG_BUILT_DEFAULT="$(pwd)/image/out/ubuntu-22.04-arm64-runtime-min-nosystemd.img"
+export CORAL_DISK_IMG_BUILT_ALT1="$(pwd)/image/out/ubuntu-22.04-arm64-runtime-min.img"
+export CORAL_DISK_IMG_BUILT_ALT2="$(pwd)/image/out/ubuntu-18.04-arm64-dev-dense.img"
+export CORAL_DISK_IMG_BUILT_ALT3="$(pwd)/image/out/ubuntu-18.04-arm64-dev.img"
+export CORAL_DISK_IMG_FALLBACK="/Users/libo/Work/disk_img/gem5_arm_linux_images/ubuntu-18.04-arm64-docker.img"
+export CORAL_DISK_IMG="${CORAL_DISK_IMG:-${IMAGE_PATH}/ubuntu-18.04-arm64-docker.img}"
+export CORAL_CKPT_ROOT="$(pwd)/m5out/coralnpu_ckpt"
+export CORAL_BOOTED_CKPT="${CORAL_CKPT_ROOT}/booted"
+export CORAL_REBUILD_CKPT="${CORAL_REBUILD_CKPT:-0}"
+export CORAL_CKPT_IMAGE_META="${CORAL_CKPT_ROOT}/disk_image_path.txt"
+export CORAL_CKPT_INIT_META="${CORAL_CKPT_ROOT}/kernel_init_path.txt"
+
+if [ "${CORAL_DISK_IMG}" = "${IMAGE_PATH}/ubuntu-18.04-arm64-docker.img" ]; then
+  for candidate in \
+    "${CORAL_DISK_IMG_BUILT_DEFAULT}" \
+    "${CORAL_DISK_IMG_BUILT_ALT1}" \
+    "${CORAL_DISK_IMG_BUILT_ALT2}" \
+    "${CORAL_DISK_IMG_BUILT_ALT3}" \
+    "${CORAL_DISK_IMG_FALLBACK}"
+  do
+    if [ -f "${candidate}" ]; then
+      CORAL_DISK_IMG="${candidate}"
+      break
+    fi
+  done
+fi
+
+if [ "${CORAL_REBUILD_CKPT}" = "1" ]; then
+  rm -rf "${CORAL_CKPT_ROOT}"
+fi
+
+if [ -d "${CORAL_BOOTED_CKPT}" ] && [ "${CORAL_CKPT_BOOTSCRIPT}" -nt "${CORAL_BOOTED_CKPT}/m5.cpt" ]; then
+  echo "Bootstrap script changed; rebuilding boot checkpoint"
+  rm -rf "${CORAL_CKPT_ROOT}"
+fi
+
+if [ -d "${CORAL_BOOTED_CKPT}" ] && [ "${CORAL_DISK_IMG}" -nt "${CORAL_BOOTED_CKPT}/m5.cpt" ]; then
+  echo "Disk image contents changed; rebuilding boot checkpoint"
+  rm -rf "${CORAL_CKPT_ROOT}"
+fi
+
+if [ -d "${CORAL_BOOTED_CKPT}" ] && [ ! -f "${CORAL_CKPT_IMAGE_META}" ]; then
+  echo "Legacy checkpoint metadata missing; rebuilding boot checkpoint"
+  rm -rf "${CORAL_CKPT_ROOT}"
+fi
+
+if [ -d "${CORAL_BOOTED_CKPT}" ] && [ -f "${CORAL_CKPT_IMAGE_META}" ]; then
+  if [ "$(cat "${CORAL_CKPT_IMAGE_META}")" != "${CORAL_DISK_IMG}" ]; then
+    echo "Disk image changed; rebuilding boot checkpoint"
+    rm -rf "${CORAL_CKPT_ROOT}"
+  fi
+fi
+
+if [ -d "${CORAL_BOOTED_CKPT}" ] && [ ! -f "${CORAL_CKPT_INIT_META}" ]; then
+  echo "Legacy kernel-init metadata missing; rebuilding boot checkpoint"
+  rm -rf "${CORAL_CKPT_ROOT}"
+fi
+
+if [ -d "${CORAL_BOOTED_CKPT}" ] && [ -f "${CORAL_CKPT_INIT_META}" ]; then
+  if [ "$(cat "${CORAL_CKPT_INIT_META}")" != "${CORAL_KERNEL_INIT}" ]; then
+    echo "Kernel init changed; rebuilding boot checkpoint"
+    rm -rf "${CORAL_CKPT_ROOT}"
+  fi
+fi
+
+mkdir -p "${CORAL_CKPT_ROOT}"
 
 # 默认启动命令d9300，可对应修改d9300 -> d9200
 #./build/ARM/gem5.opt configs/example/arm/arm_multicore_d9200.py --cpu-type="D9200" --disk=${IMAGE_PATH}/ubuntu-18.04-arm64-docker.img --kernel=${IMAGE_PATH}/vmlinux.arm64 --enable-gemmini --gemmini-cpu-idx 0
 #./build/ARM/gem5.opt configs/example/arm/arm_multicore_d9200.py --cpu-type="D9200" --disk=${IMAGE_PATH}/ubuntu-18.04-arm64-docker.img --kernel=${IMAGE_PATH}/vmlinux.arm64 --bootscript configs/gemmini/fs-run.rcS --enable-gemmini --gemmini-mmio-iobus --gemmini-ctrl-addr 0x10030000 --gemmini-ctrl-size 0x1000
-./build/ARM/gem5.opt configs/example/arm/arm_multicore_d9300.py --cpu-type="D9300" --disk=${IMAGE_PATH}/ubuntu-18.04-arm64-docker.img --kernel=${IMAGE_PATH}/vmlinux.arm64
+echo "Launching ARM multicore FS with Coral NPU stage-A enabled"
+echo "Guest terminal socket: ./util/term/gem5term localhost 4567"
+echo "Disk image: ${CORAL_DISK_IMG}"
+echo "Kernel init: ${CORAL_KERNEL_INIT}"
+echo "Checkpoint root: ${CORAL_CKPT_ROOT}"
+ls -lh "${CORAL_DISK_IMG}" || exit 1
+echo "Building build/ARM/gem5.opt with -j${BUILD_JOBS}"
+scons build/ARM/gem5.opt -j"${BUILD_JOBS}"
+
+if [ -d "${CORAL_BOOTED_CKPT}" ]; then
+  echo "Mode: restore from booted checkpoint"
+  echo "Checkpoint: ${CORAL_BOOTED_CKPT}"
+  echo "Resume script: ${CORAL_RESUME_BOOTSCRIPT}"
+  ./build/ARM/gem5.opt configs/example/arm/arm_multicore_d9300.py \
+    --cpu-type="D9300" \
+    --disk="${CORAL_DISK_IMG}" \
+    --kernel="${IMAGE_PATH}/vmlinux.arm64" \
+    --kernel-init="${CORAL_KERNEL_INIT}" \
+    --enable-npu \
+    --ckpt-dir="${CORAL_CKPT_ROOT}" \
+    --restore-from="${CORAL_BOOTED_CKPT}" \
+    --bootscript="${CORAL_RESUME_BOOTSCRIPT}"
+else
+  echo "Mode: bootstrap and create checkpoint"
+  echo "Bootstrap script: ${CORAL_CKPT_BOOTSCRIPT}"
+  echo "Set CORAL_REBUILD_CKPT=1 to force rebuilding the boot checkpoint"
+  ./build/ARM/gem5.opt configs/example/arm/arm_multicore_d9300.py \
+    --cpu-type="D9300" \
+    --disk="${CORAL_DISK_IMG}" \
+    --kernel="${IMAGE_PATH}/vmlinux.arm64" \
+    --kernel-init="${CORAL_KERNEL_INIT}" \
+    --enable-npu \
+    --ckpt-dir="${CORAL_CKPT_ROOT}" \
+    --exit-after-checkpoint \
+    --bootscript="${CORAL_CKPT_BOOTSCRIPT}"
+  printf '%s\n' "${CORAL_DISK_IMG}" > "${CORAL_CKPT_IMAGE_META}"
+  printf '%s\n' "${CORAL_KERNEL_INIT}" > "${CORAL_CKPT_INIT_META}"
+  echo "Boot checkpoint saved at ${CORAL_BOOTED_CKPT}"
+  echo "Run ./run_multicore.sh again to restore and execute the Coral NPU test script"
+fi
+
 # 按照指定slice dump启动命令
 #./build/ARM/gem5.opt configs/example/arm/arm_multicore_slc.py --slice-run --slice-ticks 10ms --disk=${IMAGE_PATH}/ubuntu-18.04-arm64-docker.img --kernel=${IMAGE_PATH}/../binaries/vmlinux.arm64 --caches --last-cache-level 3 --cpu-type="x4a720"
 #
diff --git a/src/dev/npu/NPUDevice.py b/src/dev/npu/NPUDevice.py
index 7701b4903a..4b36b63ec1 100644
--- a/src/dev/npu/NPUDevice.py
+++ b/src/dev/npu/NPUDevice.py
@@ -1,13 +1,16 @@
 # Copyright (c) 2025
 # All rights reserved.
 #
-# A minimal NPU device model interface for gem5.
+# Coral NPU stage-A model interface for gem5.
 #
-# This model is intentionally simple: it exposes an MMIO region and a
-# DMA-capable interface, but does not implement any real computation.
+# This model exposes a single contiguous MMIO aperture covering the Coral NPU
+# ITCM, DTCM, and external CSR windows. It intentionally does not execute real
+# Coral instructions yet; instead it provides the boot/control contract and a
+# fixed-latency run-to-halt state machine suitable for SoC bring-up.
 
 from m5.objects.Device import DmaVirtDevice
 from m5.params import *
+from m5.util.fdthelper import *
 
 
 class NPUDevice(DmaVirtDevice):
@@ -15,9 +18,44 @@ class NPUDevice(DmaVirtDevice):
     cxx_header = "dev/npu/npu_device.hh"
     cxx_class = "gem5::NPUDevice"
 
-    # Base address and size of the NPU MMIO window.
-    pioAddr = Param.Addr("Base physical address of the NPU MMIO region")
-    pioSize = Param.Addr(0x1000, "Size of the NPU MMIO region")
+    # Base address and size of the Coral NPU aperture.
+    pioAddr = Param.Addr("Base physical address of the Coral NPU aperture")
+    pioSize = Param.Addr(
+        0x31000,
+        "Size of the Coral NPU aperture covering ITCM/DTCM/CSR windows",
+    )
 
-    # Simple fixed MMIO latency.
-    pioDelay = Param.Latency("10ns", "Latency for NPU MMIO accesses")
+    # Stage-A Coral memory map parameters.
+    itcmSize = Param.MemorySize("8KiB", "Coral NPU ITCM size")
+    dtcmSize = Param.MemorySize("32KiB", "Coral NPU DTCM size")
+    pioDelay = Param.Latency("10ns", "Latency for Coral NPU MMIO accesses")
+    executionLatency = Param.Latency(
+        "1us", "Fixed run-to-halt latency once Coral NPU starts"
+    )
+    autoHalt = Param.Bool(
+        True,
+        "Automatically transition the stage-A Coral NPU model to halted",
+    )
+    backendType = Param.String(
+        "stage-a",
+        "Execution backend: 'stage-a' or future 'verilated-coral'",
+    )
+    coralRepo = Param.String(
+        "",
+        "Path to the external google-coral/coralnpu checkout used for RTL development",
+    )
+    verilatedWrapper = Param.String(
+        "",
+        "Path to the future Verilated Coral wrapper artifact or build directory",
+    )
+    rtlTickPeriod = Param.Latency(
+        "1ns",
+        "Backend tick quantum for the future Verilated Coral bridge",
+    )
+
+    def generateDeviceTree(self, state):
+        node = self.generateBasicPioDeviceNode(
+            state, "coralnpu", self.pioAddr, self.pioSize
+        )
+        node.appendCompatible(["google,coralnpu", "google,coralnpu-stagea"])
+        yield node
diff --git a/src/dev/npu/SConscript b/src/dev/npu/SConscript
index 011502f3a5..7364ee771f 100644
--- a/src/dev/npu/SConscript
+++ b/src/dev/npu/SConscript
@@ -10,7 +10,8 @@ Import('*')
 
 SimObject('NPUDevice.py', sim_objects=['NPUDevice'])
 
+Source('coral_stagea_backend.cc')
+Source('coral_verilated_backend.cc')
 Source('npu_device.cc')
 
 DebugFlag('NPUDevice')
-
diff --git a/src/dev/npu/npu_device.cc b/src/dev/npu/npu_device.cc
index 7e0d231f97..14460c8b76 100644
--- a/src/dev/npu/npu_device.cc
+++ b/src/dev/npu/npu_device.cc
@@ -1,9 +1,5 @@
 /*
- * Minimal NPU device interface for gem5.
- *
- * This model is a thin stub that exposes an MMIO space and a DMA-capable
- * interface via DmaVirtDevice. It does not implement any real NPU
- * functionality and is intended only to validate integration paths.
+ * Coral-compatible NPU device shell for gem5.
  */
 
 #include "dev/npu/npu_device.hh"
@@ -12,42 +8,89 @@
 #include "base/logging.hh"
 #include "base/trace.hh"
 #include "debug/NPUDevice.hh"
+#include "dev/npu/coral_stagea_backend.hh"
+#include "dev/npu/coral_verilated_backend.hh"
 
 namespace gem5
 {
 
 NPUDevice::NPUDevice(const Params &p)
-  : DmaVirtDevice(p), pioAddr(p.pioAddr), pioSize(p.pioSize),
-    pioDelay(p.pioDelay)
+  : DmaVirtDevice(p),
+    pioAddr(p.pioAddr),
+    pioSize(p.pioSize),
+    pioDelay(p.pioDelay),
+    backendEvent(*this)
 {
+    if (p.backendType == "stage-a") {
+        backend = std::make_unique<CoralStageABackend>(
+            pioAddr, pioSize, p.itcmSize, p.dtcmSize, p.executionLatency,
+            p.autoHalt);
+    } else if (p.backendType == "verilated-coral") {
+        backend = std::make_unique<CoralVerilatedBackend>(
+            p.coralRepo, p.verilatedWrapper, p.rtlTickPeriod);
+    } else {
+        fatal("Unknown NPU backend type '%s'", p.backendType);
+    }
+
+    DPRINTFR(NPUDevice, "Created NPUDevice backend=%s pio=%#x size=%#x\n",
+             backend->name(), pioAddr, pioSize);
+    syncBackendEvent();
 }
 
 void
-NPUDevice::IdentityTranslationGen::translate(Range &range) const
+NPUDevice::processBackendEvent()
 {
-    // Identity mapping: treat vaddr as paddr.
-    range.paddr = range.vaddr;
-    // Use the full remaining size of the region.
-    // range.size is already set by the TranslationGen base class.
+    backend->processEvent();
+    syncBackendEvent();
+}
+
+void
+NPUDevice::syncBackendEvent()
+{
+    if (backend->hasPendingEvent()) {
+        const Tick when = backend->nextEventTick();
+        if (!backendEvent.scheduled() || backendEvent.when() != when) {
+            if (backendEvent.scheduled()) {
+                deschedule(backendEvent);
+            }
+            schedule(backendEvent, when);
+        }
+    } else if (backendEvent.scheduled()) {
+        deschedule(backendEvent);
+    }
 }
 
 Tick
 NPUDevice::read(PacketPtr pkt)
 {
-    const Addr addr = pkt->getAddr();
-    DPRINTF(NPUDevice, "MMIO read addr=%#x size=%u\n", addr, pkt->getSize());
+    DPRINTFR(NPUDevice, "backend=%s read addr=%#x size=%u\n",
+             backend->name(), pkt->getAddr(), pkt->getSize());
 
-    pkt->makeAtomicResponse();
+    if (!backend->read(pkt, pioAddr)) {
+        DPRINTFR(NPUDevice, "backend=%s bad read addr=%#x size=%u\n",
+                 backend->name(), pkt->getAddr(), pkt->getSize());
+        pkt->makeAtomicResponse();
+        pkt->setBadAddress();
+    }
+
+    syncBackendEvent();
     return pioDelay;
 }
 
 Tick
 NPUDevice::write(PacketPtr pkt)
 {
-    const Addr addr = pkt->getAddr();
-    DPRINTF(NPUDevice, "MMIO write addr=%#x size=%u\n", addr, pkt->getSize());
+    DPRINTFR(NPUDevice, "backend=%s write addr=%#x size=%u\n",
+             backend->name(), pkt->getAddr(), pkt->getSize());
+
+    if (!backend->write(pkt, pioAddr)) {
+        DPRINTFR(NPUDevice, "backend=%s bad write addr=%#x size=%u\n",
+                 backend->name(), pkt->getAddr(), pkt->getSize());
+        pkt->makeAtomicResponse();
+        pkt->setBadAddress();
+    }
 
-    pkt->makeAtomicResponse();
+    syncBackendEvent();
     return pioDelay;
 }
 
@@ -55,18 +98,14 @@ AddrRangeList
 NPUDevice::getAddrRanges() const
 {
     AddrRangeList ranges;
-    if (pioSize != 0) {
-        ranges.push_back(RangeSize(pioAddr, pioSize));
-    }
+    ranges.push_back(RangeSize(pioAddr, pioSize));
     return ranges;
 }
 
 TranslationGenPtr
 NPUDevice::translate(Addr vaddr, Addr size)
 {
-    DPRINTF(NPUDevice, "DMA translate vaddr=%#x size=%u\n", vaddr, size);
-    return TranslationGenPtr(new IdentityTranslationGen(vaddr, size));
+    return backend->translate(vaddr, size);
 }
 
 } // namespace gem5
-
diff --git a/src/dev/npu/npu_device.hh b/src/dev/npu/npu_device.hh
index 0de7361edd..bad3adfb21 100644
--- a/src/dev/npu/npu_device.hh
+++ b/src/dev/npu/npu_device.hh
@@ -1,17 +1,21 @@
 /*
- * Minimal NPU device interface for gem5.
+ * Coral-compatible NPU device shell for gem5.
  *
- * This model is a thin stub that exposes an MMIO space and a DMA-capable
- * interface via DmaVirtDevice. It does not implement any real NPU
- * functionality and is intended only to validate integration paths.
+ * This SimObject owns the SoC-facing MMIO aperture, DMA port, and board-level
+ * integration. Execution behavior is delegated to a pluggable backend:
+ * - stage-a: current transaction-level run-to-halt stub
+ * - verilated-coral: future bridge to the official Coral RTL framework
  */
 
 #ifndef __DEV_NPU_NPU_DEVICE_HH__
 #define __DEV_NPU_NPU_DEVICE_HH__
 
+#include <memory>
+
 #include "dev/dma_virt_device.hh"
-#include "mem/translation_gen.hh"
+#include "dev/npu/coral_backend.hh"
 #include "params/NPUDevice.hh"
+#include "sim/eventq.hh"
 
 namespace gem5
 {
@@ -23,39 +27,23 @@ class NPUDevice : public DmaVirtDevice
     Addr pioSize;
     Tick pioDelay;
 
-    /**
-     * Very simple translation generator that treats virtual addresses as
-     * physical addresses (identity mapping). This is sufficient for a stub
-     * model that does not rely on OS-managed virtual memory.
-     */
-    class IdentityTranslationGen : public TranslationGen
-    {
-      public:
-        IdentityTranslationGen(Addr vaddr, Addr size)
-          : TranslationGen(vaddr, size)
-        {}
-
-      private:
-        void translate(Range &range) const override;
-    };
+    void processBackendEvent();
+    void syncBackendEvent();
+
+    std::unique_ptr<CoralBackend> backend;
+    MemberEventWrapper<&NPUDevice::processBackendEvent> backendEvent;
 
   public:
     using Params = NPUDeviceParams;
 
     NPUDevice(const Params &p);
 
-    /** MMIO read/write handlers. Currently they just acknowledge requests. */
     Tick read(PacketPtr pkt) override;
     Tick write(PacketPtr pkt) override;
-
-    /** Address range exposed on the PIO bus. */
     AddrRangeList getAddrRanges() const override;
-
-    /** DMA address translation helper. */
     TranslationGenPtr translate(Addr vaddr, Addr size) override;
 };
 
 } // namespace gem5
 
 #endif // __DEV_NPU_NPU_DEVICE_HH__
-
diff --git a/src/mem/cfi_mem.cc b/src/mem/cfi_mem.cc
index 54cf415da2..c9dcccdd17 100644
--- a/src/mem/cfi_mem.cc
+++ b/src/mem/cfi_mem.cc
@@ -75,13 +75,25 @@ CfiMemory::BlockData::unlock(Addr block_address)
 void
 CfiMemory::BlockData::serialize(CheckpointOut &cp) const
 {
-    SERIALIZE_CONTAINER(locked);
+    std::vector<uint8_t> locked_bytes;
+    locked_bytes.reserve(locked.size());
+    for (bool is_locked : locked) {
+        locked_bytes.push_back(is_locked ? 1 : 0);
+    }
+    arrayParamOut(cp, "locked", locked_bytes);
 }
 
 void
 CfiMemory::BlockData::unserialize(CheckpointIn &cp)
 {
-    UNSERIALIZE_CONTAINER(locked);
+    std::vector<uint8_t> locked_bytes;
+    arrayParamIn(cp, "locked", locked_bytes);
+
+    locked.clear();
+    locked.reserve(locked_bytes.size());
+    for (uint8_t is_locked : locked_bytes) {
+        locked.push_back(is_locked != 0);
+    }
 }
 
 uint32_t
