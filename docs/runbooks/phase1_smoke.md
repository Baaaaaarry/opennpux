# Phase 1 Smoke

Use `tools/coralnpu/phase1_bootstrap.sh` and
`tools/coralnpu/phase1_host_check.sh` to validate the Coral submodule on the
target x86 Linux machine before attempting full standalone or gem5-coupled
simulation.
