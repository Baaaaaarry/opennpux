# Coral Standalone Smoke

Use `tools/coralnpu/check_coral_checkout.sh` and
`tools/coralnpu/check_coral_host_tools.sh` to validate the Coral submodule on the
target x86 Linux machine before attempting full standalone or gem5-coupled
simulation.
