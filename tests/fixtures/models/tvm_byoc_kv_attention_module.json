{
  "format": "OPENNPUX_TVM_BYOC_MODULE_V1",
  "regions": [
    {
      "name": "kv_update",
      "graph": {
        "format": "OPENNPUX_TVM_BYOC_GRAPH_V1",
        "tensors": [
          {"name": "token_kv", "shape": [2, 1, 2], "dtype": "float32", "storage": "input"},
          {"name": "produced_kv", "shape": [2, 1, 2], "dtype": "float32", "storage": "output"}
        ],
        "nodes": [
          {"op": "copy", "inputs": ["token_kv"], "outputs": ["produced_kv"]}
        ],
        "outputs": ["produced_kv"]
      }
    },
    {
      "name": "attention",
      "graph": {
        "format": "OPENNPUX_TVM_BYOC_GRAPH_V1",
        "tensors": [
          {"name": "query", "shape": [1, 2, 2], "dtype": "float32", "storage": "input"},
          {"name": "kv_cache", "shape": [2, 2, 1, 2], "dtype": "float32", "storage": "state"},
          {"name": "context", "shape": [1, 2, 2], "dtype": "float32", "storage": "output"}
        ],
        "nodes": [
          {
            "op": "opennpux.attention",
            "inputs": ["query", "kv_cache"],
            "outputs": ["context"],
            "attrs": {"kv_length": 2}
          }
        ],
        "outputs": ["context"]
      }
    }
  ],
  "edges": [],
  "state_updates": [
    {
      "from": {"region": "kv_update", "tensor": "produced_kv"},
      "to": {"region": "attention", "tensor": "kv_cache"},
      "mode": "append_planar2"
    }
  ],
  "scalar_bindings": [
    {
      "name": "kv_length",
      "region": "attention",
      "command": 0,
      "field": "flags",
      "minimum": 1,
      "maximum": 2
    }
  ]
}
